{{ define "title"}}
<title>{{ .Post.Title }}  -  Rangi Lin's blog</title>
{{ end }}

{{ define "content"}}
<div>
  <a href="{{ .Post.Url }}"><h1>{{ .Post.Title }}</h1></a>
  <p> <i class="fa fa-calendar"></i> {{ .Post.Date }} </p>
  <div>{{ .Post.HTMLContent }}</div>
</div>
{{ end }}
